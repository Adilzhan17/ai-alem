Запуск

frontend: /frontend npm run dev

backend:

```
 /backend python -m venv venv; .\venv\Scripts\pip install -r requirements.txt; .\venv\Scripts\uvicorn app.main:app --reload
```

и .\venv\Scripts\uvicorn app.main:app --reload

```
.\venv\Scripts\uvicorn app.main:app --reload
```
